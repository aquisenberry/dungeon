// import importTilemap from "splat-ecs/lib/import-from-tiled";
// import * as tilemap from "@tiled/levels/pixel-platformer.json";
export default () => {
  // export default (game) => {
  // eslint-disable-line no-unused-vars
  // importTilemap(tilemap.default, game.entities, game.images);
};
